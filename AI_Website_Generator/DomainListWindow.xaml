<Window x:Class="AI_Website_Generator.DomainListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрирани домейни"
        Height="600"
        Width="900"
        Background="#ECF0F1"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">

    <Window.Resources>

        <!-- Effects -->
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="14" ShadowDepth="4" Opacity="0.14"/>

        <!-- Brushes -->
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#073763" Offset="0"/>
            <GradientStop Color="#0B5394" Offset="0.55"/>
            <GradientStop Color="#3498DB" Offset="1"/>
        </LinearGradientBrush>

        <!-- Cards -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="BorderBrush" Value="#E6ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource ShadowEffect}"/>
        </Style>

        <Style x:Key="SoftCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.10"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header text -->
        <Style x:Key="HeaderTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="HeaderSubTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#EAF2F8"/>
            <Setter Property="Opacity" Value="0.95"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <!-- KPI pill -->
        <Style x:Key="KpiPill" TargetType="Border">
            <Setter Property="CornerRadius" Value="999"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Background" Value="#1F4E79"/>
            <Setter Property="BorderBrush" Value="#2E86C1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style x:Key="KpiLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#D6EAF8"/>
        </Style>

        <Style x:Key="KpiValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
        </Style>

        <!-- Inputs -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F7F9F9"/>
            <Setter Property="BorderBrush" Value="#D5DEE3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Buttons -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#0B5394"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#073763"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#062B4F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#073764"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2E86C1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#21618C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="BorderBrush" Value="#D5DEE3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E6ECEF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- List styles -->
        <Style TargetType="{x:Type ListView}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E6ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Background" Value="#F7FAFC"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,9"/>
            <Setter Property="BorderBrush" Value="#E6ECEF"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="#EEF2F5"
                                BorderThickness="0,0,0,1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#EAF6FF"/>
                                <Setter Property="Foreground" Value="#0B4A74"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F7FAFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HERO HEADER -->
        <Border Grid.Row="0"
                CornerRadius="18"
                Padding="18"
                Background="{StaticResource HeaderGradient}"
                Margin="0,0,0,14"
                Effect="{StaticResource ShadowEffect}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Регистър домейни" Style="{StaticResource HeaderTitle}"/>
                    <TextBlock Text="Единна справка за обекти, домейни, пакети, достъп и отговорници."
                               Style="{StaticResource HeaderSubTitle}"/>

                    <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                        <Border Style="{StaticResource KpiPill}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Общо:" Style="{StaticResource KpiLabel}"/>
                                <TextBlock x:Name="txtKpiTotal" Text="0" Style="{StaticResource KpiValue}"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource KpiPill}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Градове:" Style="{StaticResource KpiLabel}"/>
                                <TextBlock x:Name="txtKpiCities" Text="0" Style="{StaticResource KpiValue}"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource KpiPill}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Пакети:" Style="{StaticResource KpiLabel}"/>
                                <TextBlock x:Name="txtKpiPackages" Text="0" Style="{StaticResource KpiValue}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Content="➕ Нов домейн"
                            Click="OpenAddDomain_Click"
                            Style="{StaticResource SecondaryButton}"
                            MinWidth="150"
                            Margin="0,0,10,0"/>
                    <!--<Button Content="X"
                            Click="CloseWindow_Click"
                            Style="{StaticResource GhostButton}"
                            Width="46"
                            Height="46"/>-->
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENT -->
        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Sticky toolbar -->
                <Border Grid.Row="0"
                        Style="{StaticResource SoftCard}"
                        Padding="12"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="txtSearch"
                                 Grid.Column="0"
                                 Style="{StaticResource SearchBox}"
                                 ToolTip="Търси по обект, домейн, град, имейл, МОЛ, телефон..."
                                 TextChanged="txtSearch_TextChanged"/>

                        <Button Grid.Column="2"
                                Content="🔄 Обнови"
                                Click="RefreshDomains_Click"
                                Style="{StaticResource PrimaryButton}"
                                MinWidth="140"/>

                        <Button Grid.Column="4"
                                Content="Изчисти"
                                Click="ClearFilter_Click"
                                Style="{StaticResource GhostButton}"
                                MinWidth="120"/>
                    </Grid>
                </Border>

                <!-- List -->
                <ListView Grid.Row="1" x:Name="DomainsList">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Обект" Width="180" DisplayMemberBinding="{Binding Owner}"/>
                            <GridViewColumn Header="Населено място" Width="140" DisplayMemberBinding="{Binding City}"/>
                            <GridViewColumn Header="Код НЕИСПУО" Width="120" DisplayMemberBinding="{Binding Code}"/>
                            <GridViewColumn Header="МОЛ" Width="180" DisplayMemberBinding="{Binding Mol}"/>
                            <GridViewColumn Header="Телефон" Width="130" DisplayMemberBinding="{Binding Phone}"/>
                            <GridViewColumn Header="Имейл" Width="220" DisplayMemberBinding="{Binding OwnerEmail}"/>
                            <GridViewColumn Header="Пакет" Width="110" DisplayMemberBinding="{Binding Package}"/>
                            <GridViewColumn Header="Нов домейн" Width="220" DisplayMemberBinding="{Binding NewDomainName}"/>
                            <GridViewColumn Header="Стар домейн" Width="220" DisplayMemberBinding="{Binding OldDomainName}"/>
                            <GridViewColumn Header="Admin потребител" Width="170" DisplayMemberBinding="{Binding AdminUsername}"/>
                            <GridViewColumn Header="Admin парола" Width="160" DisplayMemberBinding="{Binding AdminPassword}"/>
                            <GridViewColumn Header="Тестов домейн дата" Width="150" DisplayMemberBinding="{Binding RegTestDomainDate}"/>
                            <GridViewColumn Header="Официален домейн дата" Width="170" DisplayMemberBinding="{Binding RegOfficialDomainDate}"/>
                            <GridViewColumn Header="Хостинг (договор)" Width="150" DisplayMemberBinding="{Binding Hosting}"/>
                            <GridViewColumn Header="Поел от екипа" Width="150" DisplayMemberBinding="{Binding RequestTeamMember}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

            </Grid>
        </Border>

        <!-- FOOTER -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="ℹ️ Съвет: Използвай търсачката за бързо филтриране. Скролирай хоризонтално за всички колони."
                       Foreground="#5D6D7E"
                       FontSize="12"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="Затвори"
                    Style="{StaticResource GhostButton}"
                    Click="CloseWindow_Click"
                    Width="140"
                    Margin="10,0,0,0"/>
        </Grid>

    </Grid>
</Window>
