<Window x:Class="AI_Website_Generator.ManageDesignsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Темплейти"
        Height="520"
        Width="980"
        Background="#ECF0F1"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="14" ShadowDepth="4" Opacity="0.14"/>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderBrush" Value="#E6ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource ShadowEffect}"/>
        </Style>

        <Style x:Key="HeaderTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <Style x:Key="HeaderSubTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#5D6D7E"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#0B5394"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#073763"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#062B4F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#3498DB"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2E86C1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#21618C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="BorderBrush" Value="#D5DEE3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E6ECEF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F7F9F9"/>
            <Setter Property="BorderBrush" Value="#D5DEE3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="{x:Type ListBox}">
            <Setter Property="BorderBrush" Value="#E6ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderBrush="#EEF2F5"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#EAF6FF"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#BEE6FD"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F7FAFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="🎨 Управление на темплейти" Style="{StaticResource HeaderTitle}"/>
                    <TextBlock Text="Избор, преглед и бърз достъп до HTML темплейтите за деплой."
                               Style="{StaticResource HeaderSubTitle}"/>
                </StackPanel>

                <!--<Button Grid.Column="1"
                        Content="✖"
                        Style="{StaticResource GhostButton}"
                        Width="46"
                        Height="46"
                        Click="Close_Click"
                        Margin="12,0,0,0"/>-->
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: list -->
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Списък темплейти" FontSize="16" FontWeight="Bold" Foreground="#2C3E50"/>

                        <Button Grid.Column="1"
                                Content="🔄"
                                Style="{StaticResource GhostButton}"
                                Width="44"
                                Height="40"
                                Click="Refresh_Click"
                                ToolTip="Презареди списъка"/>
                    </Grid>

                    <TextBox Grid.Row="1"
                             x:Name="txtSearch"
                             Style="{StaticResource SearchBox}"
                             Margin="0,12,0,12"
                             ToolTip="Търси по име (A/B/C) или описание..."
                             TextChanged="txtSearch_TextChanged"/>

                    <ListBox Grid.Row="2"
                             x:Name="TemplateList"
                             SelectionChanged="TemplateList_SelectionChanged">
                        <ListBoxItem Tag="templates/templateA.html">Template A — University</ListBoxItem>
                        <ListBoxItem Tag="templates/templateB.html">Template B — Kindergarden</ListBoxItem>
                        <ListBoxItem Tag="templates/templateC.html">Template C — School</ListBoxItem>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Right: details -->
            <Border Grid.Column="2" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Детайли" FontSize="16" FontWeight="Bold" Foreground="#2C3E50"/>

                    <StackPanel Grid.Row="1" Margin="0,12,0,0">
                        <TextBlock Text="Избран темплейт:" FontWeight="SemiBold" Foreground="#34495E"/>
                        <TextBlock x:Name="txtSelectedName" Text="(не е избран)" Foreground="#2C3E50" FontSize="14" Margin="0,4,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,12,0,0">
                        <TextBlock Text="Път:" FontWeight="SemiBold" Foreground="#34495E"/>
                        <TextBox x:Name="txtSelectedPath"
                                 IsReadOnly="True"
                                 Text=""
                                 Background="#F7F9F9"
                                 BorderBrush="#D5DEE3"
                                 BorderThickness="1"
                                 Padding="10,8"
                                 Height="38"/>
                    </StackPanel>

                    <Border Grid.Row="3" Margin="0,14,0,0" Background="#F7FAFC" CornerRadius="12" Padding="14" BorderBrush="#E6ECEF" BorderThickness="1">
                        <TextBlock x:Name="txtStatus"
                                   Text="Избери темплейт отляво, за да видиш информация и действия."
                                   Foreground="#5D6D7E"
                                   TextWrapping="Wrap"/>
                    </Border>

                    <!-- Actions -->
                    <Grid Grid.Row="4" Margin="0,14,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="📂 Отвори"
                                Style="{StaticResource PrimaryButton}"
                                Click="OpenSelected_Click"
                                MinWidth="120"/>

                        <Button Grid.Column="2"
                                Content="🗂️ Папка"
                                Style="{StaticResource SecondaryButton}"
                                Click="OpenFolder_Click"
                                MinWidth="120"/>

                        <Button Grid.Column="4"
                                Content="📋 Copy path"
                                Style="{StaticResource GhostButton}"
                                Click="CopyPath_Click"
                                MinWidth="120"/>

                        <Button Grid.Column="6"
                                Content="Затвори"
                                Style="{StaticResource GhostButton}"
                                Click="Close_Click"
                                MinWidth="120"/>
                    </Grid>

                </Grid>
            </Border>
        </Grid>

        <!-- Footer -->
        <TextBlock Grid.Row="2"
                   Margin="6,12,6,0"
                   Text="ℹ️ Препоръка: Отваряйте темплейта през бутона “Отвори”, вместо автоматично при избор."
                   Foreground="#5D6D7E"
                   FontSize="12"/>
    </Grid>
</Window>
